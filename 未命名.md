# CUDA 开卷考试快速查询表 Demo：极简大纲版

## Lecture 4：Memory Model

| 模块 / Module                                  | 关键词 / Keywords                                                                                                                               | 中文说明                                                                                                                                                                      | PPT 页码       |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Von-Neumann Model and Instruction Processing | Von-Neumann model, memory, input/output, control unit, processing unit, PC, IR, ALU, register file, fetch, decode, execute, memory, ADD, LDR | 冯诺依曼模型与指令执行流程。了解计算机由内存、输入输出、控制单元、处理单元构成；指令需要经历取指、译码、执行、访存等阶段；ADD 是运算指令，LDR 是访存指令。                                                                                         | Lec4 p3-p6   |
| Registers vs Memory                          | registers, memory, latency, capacity, 1 cycle, hundreds of cycles                                                                            | 寄存器速度快但容量小，内存容量大但访问慢。这是后面 CUDA 内存层次和性能优化的基础背景。                                                                                                                            | Lec4 p7      |
| CUDA Memory Hierarchy                        | register, shared memory, global memory, constant memory, scope, lifetime, thread, block, grid, application                                   | CUDA 内存层次。重点区分 register、shared memory、global memory、constant memory 的访问范围、生命周期和速度：register 是线程私有，shared memory 是 block 内共享，global memory 所有线程可见但慢，constant memory 只读且可缓存。 | Lec4 p8-p9   |
| Matrix Multiplication Basics                 | matrix multiplication, M, N, P, row, column, dot product, Width, sequential C code, outer loops, inner loop                                  | 矩阵乘法基础。每个输出元素 `P[i][j]` 是 M 的第 i 行和 N 的第 j 列做点积。CPU 代码是三重循环，CUDA 并行化时通常先并行外层 i、j 循环，让一个线程计算一个输出元素。                                                                        | Lec4 p10-p13 |
| Floating-Point Non-Associativity             | floating-point, not associative, rounding error, reduction, order of addition, `2^-30`, precision                                            | 浮点数不严格满足结合律。由于精度有限和舍入误差，改变加法顺序可能导致结果不同，因此并行 reduction 或改变求和顺序时，浮点结果可能与串行结果略有差异。                                                                                           | Lec4 p14-p15 |
| 2D Grid and Block for Matrix Multiplication  | 2D grid, 2D block, TILE_WIDTH, tile, Row, Col, blockIdx, threadIdx, dimGrid, dimBlock                                                        | 用二维 grid 和二维 block 计算矩阵乘法。输出矩阵是二维结构，所以线程也按二维组织；每个 block 计算一个 tile，每个线程负责 tile 中一个输出元素。                                                                                    | Lec4 p16-p20 |
| MatrixMulKernel                              | MatrixMulKernel, Row, Col, `d_M[Row * Width + k]`, `d_N[k * Width + Col]`, `d_P[Row * Width + Col]`, boundary check                          | 矩阵乘法 kernel。线程根据 `blockIdx` 和 `threadIdx` 计算自己的 `Row` 和 `Col`，再循环 k 累加乘积，最后写回 `P[Row][Col]`。重点看三个 row-major 下标表达式。                                                        | Lec4 p21-p25 |
| Global Memory Bottleneck                     | global memory access, memory bandwidth, latency, reuse, memory-bound, performance implication                                                | 基础矩阵乘法的性能瓶颈。每个线程反复从 global memory 读取 M 和 N，global memory 延迟高、带宽有限，因此程序可能受内存带宽限制。后续 shared memory tiling 就是为了解决这个问题。                                                       | Lec4 p26-p30 |
| Problem Solving                              | blocks, 2D matrix, shared memory, same block, nondeterministic write, multiple blocks, same location                                         | 习题部分。重点包括：二维矩阵需要多少 block；同一 block 内线程共享数据应使用 shared memory；多个 block 同时写同一地址时，由于 block 执行顺序不确定，最终结果也不确定。                                                                   | Lec4 p32-p34 |